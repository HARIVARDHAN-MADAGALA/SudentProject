spring.application.name=student-service
server.port=8080

#DB
#spring.datasource.url=${DB_SQL_URL}
#spring.datasource.username=${DB_USERNAME}
#spring.datasource.password=${DB_PASSWORD}
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

#address-service.url=http://localhost:8082



# CIRCUIT BREAKER
resilience4j.circuitbreaker.instances.address-service.registerHealthIndicator=true
resilience4j.circuitbreaker.instances.address-service.slidingWindowSize=5
resilience4j.circuitbreaker.instances.address-service.minimumNumberOfCalls=3
resilience4j.circuitbreaker.instances.address-service.failureRateThreshold=50
resilience4j.circuitbreaker.instances.address-service.waitDurationInOpenState=10s

# RETRY
resilience4j.retry.instances.address-service.maxAttempts=3
resilience4j.retry.instances.address-service.waitDuration=2s

#feign_connecting_with_circuitbreaker
spring.cloud.openfeign.circuitbreaker.enabled=true

#admin
#spring.boot.admin.client.url=${ADMIN_SERVER_URL}
#spring.boot.admin.client.instance.service-base-url=${SERVICE_BASE_URL}
#spring.boot.admin.client.instance.health-url=${SERVICE_BASE_URL}/actuator/health


management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

management.endpoint.metrics.enabled=true
management.endpoint.env.enabled=true
management.endpoint.beans.enabled=true
management.endpoint.mappings.enabled=true
management.endpoint.loggers.enabled=true

management.endpoints.web.base-path=/actuator

#Jwt
jwt.secret=MyVerySecretKey1234567890MyVerySecretKey!!
jwt.expiration=3600000





